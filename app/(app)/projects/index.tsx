import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import ProjectsView from '@/src/_views/ProjectsView'

export default function index() {
    return (
        <ProjectsView />
    )
}

const styles = StyleSheet.create({})